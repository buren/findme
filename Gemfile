source 'http://rubygems.org'

gem 'rails', '3.1.0'
# Database
gem 'sqlite3'

group :assets do
  gem 'sass-rails', "  ~> 3.1.0"
  gem 'coffee-rails', "~> 3.1.0"
  gem 'uglifier'
end

gem 'jquery-rails'

# Alternative to WebBrick
gem 'thin'

# Similar functionality as pjax 
gem 'turbolinks'

# For using has_secure_password
gem 'bcrypt-ruby', '~> 3.0.0'

group :test do
  # Pretty printed test output
  gem 'turn', :require => false
end
